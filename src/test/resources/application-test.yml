spring:
  datasource:
    url: jdbc:mariadb://localhost:3308/testdb 
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop 
    show-sql: true 
  test:
    database:
      replace: none 
logging:
  level:
    org:
      springframework:
        test: DEBUG 

                       
