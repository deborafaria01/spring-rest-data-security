spring:
  datasource:
    url: jdbc:mariadb://localhost:3308/test_sample 
    username: testuser 
    password: testpassword 
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  test:
    database:
      replace: none
logging:
  level:
    org:
      springframework:
        test: DEBUG

                       
